const  {Form} = require('./components/Form.jsx')
const {expect} = require('chai')


test('should submit an object {steps:[{step:""}]}', () => {
    const steps = {steps: [{step:'pour water'}]}
    e

});